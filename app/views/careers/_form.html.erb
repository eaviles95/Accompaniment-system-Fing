
<div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12">	
          <div class="card mb-3">
            <div class="card-body">


              <%= form_with(model: career, local: true) do |form| %>
              <div class="form-group col-md-3"> 
                <% if career.errors.any? %>
                  <div id="error_explanation">
                    <h2><%= pluralize(career.errors.count, "error") %> prohibited this career from being saved:</h2>

                    <ul>
                    <% career.errors.full_messages.each do |message| %>
                      
                      <li><%= message %></li>
                    <% end %>
                    </ul>
                  </div>
                <% end %>

                <div class="field">
                  <%= form.label :"Código de la carrera" %>
                  <%= form.text_field :code, :required => true, class: "form-control" %>
                </div>

                <div class="field">
                  <%= form.label :"Nombre" %>
                  <%= form.text_field :name, :required => true, class: "form-control" %>
                </div>

                <div class="field">
                  <%= form.select(:department_id, options_from_collection_for_select(Department.all, :id, :name ),:label=>"Departamento",:required => true) %>
                </div>

                <div class="actions">
                  <%= form.submit "Guardar", class: "btn btn-primary"  %>
                </div>
              <% end %>
              </div>
            </div>
          </div>  
        </div>
</div> 